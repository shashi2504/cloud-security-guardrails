apiVersion: 1

datasources:
  - name: CloudWatch
    type: cloudwatch
    jsonData:
      authType: keys        # resolves via the mounted ~/.aws profile
      defaultRegion: ap-south-1
    editable: false
